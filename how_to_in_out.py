# for some old USACO questions, file input/output is used instead of standard input/output
# if that is the case, you will need this code (replace the file in and out with the actual file name)
# you can see the type of input next to "INPUT FORMAT" on each question

import sys

# uncomment this when submitting, comment when running in regular coding environment (IDE)
sys.stdin = open('FILE_IN_NAME.in', 'r')
sys.stdout = open('FILE_OUT_NAME.out', 'w')